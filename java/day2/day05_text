1. 객체 지향 프로그래밍(Object Oriented Programing)
    -부품(객체)를 프로그램에 반영한 것
     1) 클래스 정의
        >상태 또는 속성, 필드를 정의함
         ex)
             class Cat{   //추상화,캡슐화
                  String name;
                  String breeds;
                  double weight;

                  void claw(){
                  }
                  void meaw(){
                  }
2. OOP의 주요 특징
     1)추상화(Abstraction)  : 어떤 객체에서 주된 특징(속성)과 기능(메소드)을 부각시켜 펴현하고, 나머지 부분은 과감히 생략하는 것
     2)은닉화(Encapsulation)
     3)다형성(Polymorphism)
     4)상속형(Inheritance)
